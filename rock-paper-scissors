<!DOCTYPE html>
<html>
<!--1=rock 2=paper 3=scissors -->
<script>
  function computerPlay(min,max){ 
    return Math.floor(Math.random()*(max-min+1))+min;
  }
  const playerSelection = 'PaPer';
  const computerSelection = computerPlay(1,3);
  function playRound(playerSelection,computerSelection) {
    
    let x = computerSelection;
    console.log("Computer plays "+ playConversion());
    
    function playConversion() {
      if (x == 2) {
      return "paper";
      } else if (x > 2) {
      return "scissors";
      } else { 
      return "rock";
      }
    }
    
    let y = playerSelection.toLowerCase();
    console.log("You played " + y)
    
    function playConversionHum() {
      if (y == "rock") {
      return (c = 1);
      } else if (y == "paper") {
      return (c = 2); 
      } else if (y == "scissors"){
      return (c = 3);
      } else {
      return ("Make a play!")
      }
    }
    
    function 
  }
  
  

  console.log(playRound(playerSelection,computerSelection));
</script>
</html>

<!--rock => 1
1 needs to be compared

  let x = computerPlay(1,3);
  console.log(x)
  function playConversion() {
    if (x == 2) {
    return "paper";
    } else if (x > 2) {
    return "scissors";
    } else { 
    return "rock";
    }
}if (playerSelection == '/rock/i') {
      (1 > 2)
      else if (1 < 3)
      else if (1 == 1) -->
